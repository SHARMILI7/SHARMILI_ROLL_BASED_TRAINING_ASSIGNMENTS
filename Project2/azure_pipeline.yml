trigger:
- main

pool:
  vmImage: 'windows-latest'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.x'

- script: pip install pandas
  displayName: 'Install Pandas'

- script: python week5_inventory_report.py
  displayName: 'Run Daily Inventory Reorder Report'